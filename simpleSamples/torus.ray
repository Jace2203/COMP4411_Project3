SBT-raytracer 1.0

// cyl_cyl_refract.ray
// Test the refraction term
// Don't forget to increase the trace depth to >= 2!

camera
{
	position = (0, 5, -2);
	viewdir = (0, -5, 2);
	updir = (0, 1, 0);
}

directional_light
{
    direction = (-1,-3,1);
	colour = (1.0, 1.0, 1.0);
}

directional_light
{
    direction = (1,-3,1);
	colour = (1.0, 1.0, 1.0);
}

scale(0.5,0.5,0.5,
torus {
	radius = 0.9;
	ring_radius = 0.3;
	material = { 
		diffuse = (1.0, 0.5, 0.7);
	}
})